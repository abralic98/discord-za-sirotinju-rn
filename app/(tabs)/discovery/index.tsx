import { TextSm } from "@/lib/typography";
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function DiscoveryPage() {
  return (
    <SafeAreaView>
      <View>
        <TextSm>discovery</TextSm>
      </View>
    </SafeAreaView>
  );
}
